# SPEC — イチオシ採用 機能仕様

> 機能・ページ・動作の単一の正本。**機能追加・動作変更のたびに同一コミット内で更新。** 最終更新: 2026-09-23

## TL;DR

イチオシ採用（採用LP）の仕様。現状は新規立ち上げ前段階で、サービス定義は `TBD` を埋めていく。

## 1. サービス概要

| 項目 | 内容 |
|------|------|
| サービス名 | イチオシ採用（旧名リクルートX。GitHubリポジトリ名・内部識別子は recruitx のまま） |
| 一言説明 | 会社の魅力を、人にもAIにも届ける採用（コンテンツ採用）。出典は社内の最新営業資料（非公開・リポ外管理） |
| ターゲット | `TBD` |
| 提供価値 | 1回の取材から漫画・アニメ・記事・掲載ページを制作し、求人広告・応募前・選考中・面接前・AI検索の5接点で使い分ける。課題に応じて A 求人広告の改善 / B 応募課金型の改善（SNS運用・Meta広告運用・求人広告運用代行を含む）/ AI・検索対策 の3ルート |
| ドメイン | `ichioshi.contentsx.jp` |

## 2. ページ構成

| ページ | ファイル | 主要JS | 状態 |
|--------|---------|--------|------|
| トップLP | `index.html` | `js/main.js`（ナビsticky/ハンバーガー/入場アニメ/FAB） | 実装済み（2026-09-23 営業資料準拠でリデザイン。スタイルは `css/home.css`） |
| お問い合わせ | `contact.html` | `js/main.js`（HubSpot Forms API v3 直送 ＋ CRM受信箱へ並行POST。§7） | 実装済み（2026-08-04 再構築。**ファイルが0バイト＝本番も真っ白の状態だった**のを復元。詳細は [BUGS.md](../../BUGS.md)） |
| 導入事例 | `case.html` | `js/main.js` + `js/case.js`（業種フィルタ） | 実装済み |
| 料金プラン | `price.html` | `js/main.js`（入場アニメのみ） | 実装済み |

## 3. URL構造・ナビゲーション

- **共通ヘッダーナビ（全ページ統一・ブランド標準 `.rx-nav`）**: メニュー＝サービス / 事例 / 料金プラン / お役立ち情報 / コラム、CTA＝お問い合わせ（**ホームのみ「無料診断を申し込む」**→ `contact.html?source=free-diagnosis`。≤480pxは「無料診断」に短縮）。見た目は明朝ロゴ＋テキストリンク＋淡ピンクCTA（旧 `.rx-casepage` のモック準拠ナビは廃止）
  - 現在ページは `aria-current="page"` でピンク表示（事例ページの「事例」など）
  - 「料金プラン」は `price.html` へ遷移（2026-06-12 作成。全ページのヘッダー/フッターで `href` 有効化済み）。「お役立ち情報」「コラム」はページ未作成のため `href` 無し＋`aria-disabled="true"` のプレースホルダー（薄色・非遷移）。ページ作成時に `href` を付与
  - 「サービス」は index 内アンカー `#service`（他ページからは `index.html#service`）
- ホームのセクション構成とアンカー（2026-09-23 リデザイン。主CTAはすべて「無料診断を申し込む」→ `contact.html?source=free-diagnosis`）:
  1. `#hero` ヒーロー（「会社の魅力を、人にもAIにも届ける採用へ」＋写真2枚の重ね。PC=2カラム / ≤1024px=縦積み）
     - **動き**: 読み込み時に見出し3行＋コピーがカスケードでフェードイン、主役写真は下から拭うように出て（`clip-path`）その後12秒かけて `scale: 1→1.12`、装飾円は常時ゆっくり上下に漂う。スクロールでは写真 -60px / 副写真 +90px / 円 ±80〜120px の視差。視差は `js/main.js` がヒーローの進行度を CSS 変数 `--rx-hero-shift`(0〜1) に流し、動きの実体は home.css（transform のみでレイアウトは揺らさない）。`prefers-reduced-motion: reduce` では JS・CSS とも一切動かさない
  2. 実績の帯（応募数1.8倍・返信率58%・CPA約1/3・理解度79%。※自社調べ 2025年度実績・サンプル限定）
  3. 課題ナビ（3カード → `#route-a` / `#route-b` / `#route-ai` へページ内移動）
  4. 市場の変化（アイブロウ `MARKET`。**写真は左**・縦比のまま角丸＋出典付き調査データ3点。出典の記載がない数値は載せない）
  5. `#service` 仕組み（アイブロウ `HOW IT WORKS`。**写真は右**・画面右端までブリード＋「取材 → 4コンテンツ → 5接点」の図。4と左右を鏡写しにしてセクションの切れ目を明示。右ブリードは画面外へ伸びるため `.rx-home-mech { overflow-x: clip }` で切り取る。ナビ「サービス」と他ページの `index.html#service` の着地点）
  6. `#routes` 3つの改善ルート（`#route-a` / `#route-b` / `#route-ai`。各「課題・行うこと・向く状態」）
  7. 導入事例（case.html の既存事例から3件を静的掲載。**2カラム**で1件目が2カラムぶち抜きの大カード＝右下に空きを作らない。画像は cms.contentsx.jp → index の CSP `img-src` に許可済み）
  8. `#plans` 料金（ライト19.8万 / スタンダード34.8万・おすすめ / フル58.9万。月額・広告費別建て）
  9. 導入の流れ（4ステップ）＋既存施策を止めずにチームで進める
  10. 最終CTA（中央揃えの白パネル。写真は置かない＝申し込みに集中させる）

  **セクションの見分け**: 背景は白とごく薄いグレーを交互に。写真のあるセクションは英字アイブロウ（`MARKET` / `HOW IT WORKS`）で始め、写真の左右を交互（4=左・5=右）にして、別セクションであることを明示する。セクション上下の余白は `clamp(88px, 11vw, 172px)`。

  **ホームの写真（`images/home/`）**: `hero-reading.webp`（ヒーロー主役。LCP対象＝`preload`＋`fetchpriority="high"`）/ `interview.webp`（ヒーロー副・仕組み）/ `research.webp`（市場の変化）/ `cta-candidate.webp`（未使用。最終CTAから写真を外したため予備）。いずれも生成画像を `images/png/` から書き出したもの。
  - `hero-reading.webp` は素材写真のスマホ画面を**合成で差し替え済み**。元素材の画面は営業漫画（ビズマンガ）の訴求で採用と food が合わないため、ビズマンガ自社採用漫画「ママさん採用」のコマをSNS投稿風に組み直し、画面の四隅に透視変換で合成（画面にかかる親指は元写真を前面に残す）。差し替え手順を再実行する場合は作業用スクリプトが必要（リポジトリには含めない）
  - 旧構成（ABOUT / お悩み / 3ステップ / PLUS求人広告運用代行、sticky scrubヒーロー）は廃止。旧CSSは style.css から削除、`js/main.js` のヒーロー scrub も削除
  - よくある質問は資料・既存ページに一般向けの内容が無いため未掲載
- 導入事例は別ページ `case.html`（ナビ「事例」から遷移。`#case` アンカーは廃止）
  - デザインの出典: `採用/資料/デザイン候補/事例.png` のモックを再現し、青→ピンク `#ec2d87`／黒系 `#0a0a0c` に置換
  - 構成: ページヘッダー（右に写真・左へ白フェード）→ 業種フィルタ（すべて/飲食/清掃・家事代行）→ カード11枚（写真左38%・指標タイル3枚が写真に重なる）→ CTA（contact.html へ）
  - コンテナ幅（`.rx-chero__inner`/`.rx-clist__inner`）は `clamp(1200px, 92vw, 1760px)` で**広い画面ほど横を広く使う**（中央寄りすぎ解消）。ヘッダー写真は `min(56%, 1100px)`
  - 事例データは HTML 静的記述（出典: `採用/資料/事例/` の企画書PDF 3本）。数値は支援当時の実績
  - カード写真は `images/case/case-01〜11-*.jpg`（1448×1086 / sips jpeg 82%）。元PNGと未使用素材は `採用/資料/イメージ/` にアーカイブ
  - ヘッダーナビは全ページ共通（上記「共通ヘッダーナビ」を参照）。事例ページ固有のナビ上書きは廃止
  - 「詳しく見る」は詳細ページ未作成のためリンクではなく表示のみ（`p` 要素）
- お問い合わせは別ページ `contact.html` → ナビCTAから遷移
- 料金プランは別ページ `price.html`（ナビ「料金プラン」から遷移）
  - デザインの出典: ヒーロー＝`採用/資料/デザイン候補/料金ページ/１最新版.png`（2026-06-12 差し替え版）/ 以下＝同フォルダ `2〜5.png`
  - 構成: ①ヒーロー（**右半分に打ち合わせ写真をフルブリード背景で敷き、その上に浮遊カード2枚を重ねる**。左コピー「まずは30分、今の採用導線を一緒に整理しませんか？」＋CTA＋安心3ピル／写真上に「スタンダードプラン ¥98,000/月〜・人気No.1」カード＋「導入実績500社以上」カード。**PCは写真を右54vw(上限1500px)・ヘッダー(固定72px)の下から下端いっぱい(フル高さ)に絶対配置（写真下に白帯を作らない）。コピーを詰めてセクションを低く保つことで cover の表示窓を広げ、`object-position: 94% 50%` で女性の顔も右上の焼き込みカードも切らずに収める。左下を丸くくり抜く（白オーバーレイは使わない）。コピーは中央寄せ枠をやめ画面左に詰め、幅・見出しフォント・本文を画面比率に応じて拡大（見出しはPCで最大68px=4.25rem、コピー幅最大880px、本文/ラベルも拡大。min-heightは43vw(上限1040px)で写真を下端まで使い切る＝下の白を減らす（高くしすぎると窓が狭まり顔が切れるため上限を抑制）。コピーの行間/余白を詰めて表示窓を確保）／SP・タブレット(≤960px)はコピー→フル幅写真バナー(16:9全体表示)の縦積み。安心3ピルは≤560pxのみ2段折返し。スタンダードプランのカードは生成画像側（hero-meeting.webpに焼き込み）で対応。プログラム側の浮遊カードは「導入実績500社以上」のみ＝**PCは写真下部中央に注記つき／SP・タブレットはバナーが浅いため注記を隠した横長pillにして下端中央へ（焼き込みカード=右上・顔と重ならない）**）→ ②料金プラン4カード（A:フルスタート型 / B:広告強化型=人気No.1・ピンク枠 / C:コンテンツ単体型 / D:フリージョブ併用型）＋無料相談帯 → ③費用対効果（従来の求人広告 vs イチオシ採用 比較・20〜50%振り替え）→ ④導入5ステップ（薄グレー大パネル・点線コネクタ）→ ⑤最終CTA（白→ピンクグラデ背景・安心3ピル）
  - ⚠️ ヒーローのコピーは⑤最終CTAと同一（LPのCTAブックエンド型）。ヒーローの「¥98,000/月〜 スタンダードプラン」「導入実績500社以上」はモック由来の**新規の訴求文言**で、②のプランA〜D（40万/30万/25万…）や他資料に対応値が無い → 公開前に実数値の確認が必要
  - **旧ヒーローの3問プラン診断は廃止**（2026-06-12 最新版ヒーローへ差し替え時に削除。`js/price.js` も削除。復元はgit履歴 `b3d1675` 直後の版から）
  - ヒーロー写真は `images/price/hero-meeting.webp`（1672×941・16:9。**スタンダードプランのカードを右上に焼き込んだ完成画像**を使用。差し替え時は同比率で。PCは object-position 右寄せでカードを切らずに表示）
  - プランカードのアイコンは `images/price/plan-*.webp` 10枚（出典: `採用/資料/デザイン候補/料金ページ/素材/` のPNGを240px・WebP化）
  - 金額の出典: `採用/資料/SNSソーシャルリクルーティング_ご提案書パワポ.pdf`（プランA〜D。Cの漫画単価は 16,600円〜 に手動調整済み）
  - ページ専用スタイルは `css/price.css`（`--rxp-*` トークン。ヒーローのみ鮮やかピンク `--hp #f00858` をセクションスコープで使用。他セクションのピンクは `#eb2a63` 系）
- 新規LPは公開時に既存ページから内部リンク5〜10本 → [../content/SEO.md](../content/SEO.md)

## 4. URLパラメータ・特殊モード

- `contact.html?source=free-diagnosis`: ホームの無料診断CTAの遷移先。フォームは改修せず、`js/main.js` が `source` を「参照ページ: free-diagnosis」として問い合わせ本文末尾のトラッキング情報に書き添える（営業側で無料診断の申し込みと判別できる）

## 5. 共通コンポーネント

- **共通フッター（全ページ複製・同一マークアップ `.rx-footer`）**: index / case / contact / price の4ページに**同一の`<footer>`ブロックを静的複製**（ヘッダー同様ビルドなし・`file://`で解決）。濃紺地（`--rx-text`）＋ブランドグラデの細い上線（`::before`）。
  - 構成: 上段＝ブランド（`Recruit X`明朝ロゴ＋タグライン）｜フッターナビ、下段＝会社情報＋著作権（罫線区切り）
  - フッターナビはヘッダーと同期: サービス（`index.html#service`）/ 事例（`case.html`）/ お問い合わせ（`contact.html`）/ 料金プラン（`price.html`）＋ お役立ち情報・コラムは `href`無し＋`aria-disabled="true"` の準備中プレースホルダー（薄表示・非遷移）
  - 会社情報: 「イチオシ採用」＋「運営：コンテンツエックス株式会社 ／ Contents X Co., Ltd.」、著作権「© 2026 Contents X Co., Ltd.」
  - SNS・法的リンク（プライバシーポリシー/特商法）・コーポレートサイトリンクは**未確定のため当面は非掲載**。URL確定後に追加（追加時はこの節を更新）
  - SP（≤640px）は上段・下段とも縦積み
- **フローティングお問い合わせボタン（`.rx-fab`・全ページ共通／お問い合わせページを除く）**: 画面左下に固定し**常時表示・スクロールで消えない**（上下どちらにスクロールしても追従。`contents-x.co.jp` の追従CTAに倣う）。入場フェードのみ初回1度再生。
  - `js/main.js` が `<body>` 直下へ**DOM生成で挿入**（innerHTML不使用）。`#rx-contact-form` がある `contact.html` では挿入しない（＝自ページでは出さない）ため、ページ追加時も自動で付与・除外される
  - 見た目はブランドグラデのpill（メールアイコン＋「お問い合わせ」）。`href="contact.html"`。**文言と遷移先は `<body data-fab-label="…" data-fab-href="…">` で上書き可**（ホームは「無料診断を申し込む」→ `contact.html?source=free-diagnosis`）。z-indexは `--z-fab`。`env(safe-area-inset-bottom)` 対応・`prefers-reduced-motion`/`print` 配慮
  - スタイルは `css/style.css` 末尾の `.rx-fab`（`.is-visible` で表示）
- ヘッダー / CTA / モーダル 等の仕様: `TBD`
- デザイン値は [DESIGN.md](DESIGN.md)、表示崩れ防止は [DEVICE-RULES.md](DEVICE-RULES.md) に従う

## 6. i18n（多言語対応）

- 対応有無: `TBD`
- 実装する場合の既存パターン（Contents X 系共通）:
  - 2層構造: JSON辞書（テキスト走査で日→英置換）+ `data-ja`/`data-en`（辞書より優先）
  - スクリプト読込順序は **`i18n → nav` を厳守**（崩すと言語切替が壊れる）
  - `translateAll()` は言語判定してから呼ぶ（無条件呼び出し禁止）
  - localStorageキーは `rx-lang`（サービス専用）
  - EN は日本語より約30%長い前提でナビ/ボタン幅を設計

## 7. 外部サービス連携

- 設定値の正本は [../operations/INFRA.md](../operations/INFRA.md)。ここでは「どの連携を使うか」のみ管理。
- 使用予定: GA4（新規）/ Clarity（新規）/ HubSpot フォーム（`TBD`）

| 連携先 | 用途 | 実装 |
|---|---|---|
| HubSpot Forms API v3 | お問い合わせの受付（**主**） | `js/main.js` から直送。仕様の正本は [../operations/HUBSPOT-CTA-HANDOFF.md](../operations/HUBSPOT-CTA-HANDOFF.md) |
| Contents X CRM | お問い合わせをCRMの受信箱へ連携（2026-08-04 追加。3サイト目） | `js/main.js` の HubSpot送信**直前**に `https://contentsx-crm.vercel.app/api/inbound/web` へも POST（`CRM_ENDPOINT` / `CRM_TOKEN` 定数、`site: "ichioshi"`）。**CRM送信が失敗してもHubSpot送信とサンクス表示は従来どおり動く**（`.catch` で握りつぶす＝送信者に影響させない）。CRM側は受信箱に溜めるだけで、担当者が `/inbox` で承認して初めて会社・担当者・活動が作られる。ハニーポット `#website`（画面外・`.rx-form__hp`）の値は `hp` として送り、埋まっていればCRM側が黙って破棄する。`CRM_TOKEN` は静的サイトのJSに埋まる＝**機密ではない**（総当たり抑止の門番。実質の対策はCRM側のIPレート制限とハニーポット）。⚠️ **本サイトだけ `contact.html` の CSP `connect-src` に送信先を明示している**（他2サイトは `https:` 許容のため不要）。送信先を変える時はCSPも同時に直す。⚠️ **トークンをローテーションする時は【4箇所】を同時に更新する**（①CRM側 Vercel の `INBOUND_SECRET`＝更新後に再デプロイ ②BizManga `contact.html` ③ContentsX `js/contact.js` ④本サイト `js/main.js`）。一部だけだとそのサイトのCRM送信が全件401で落ちるが、**HubSpot受付とサンクス表示は正常なまま＝気づきにくい**。なお ContentX_HP 側の `crm-token-sync` フックは別リポジトリの本サイトを検知できない |

- 📌 `crm.contentsx.jp` 割当後は `CRM_ENDPOINT`・`contact.html` のCSP・本表を差し替える。

## 8. デプロイ

- GitHub Pages / CNAME `ichioshi.contentsx.jp` / DNS お名前.com
