# SPEC — リクルートX 機能仕様

> 機能・ページ・動作の単一の正本。**機能追加・動作変更のたびに同一コミット内で更新。** 最終更新: 2026-06-12

## TL;DR

リクルートX（採用LP）の仕様。現状は新規立ち上げ前段階で、サービス定義は `TBD` を埋めていく。

## 1. サービス概要

| 項目 | 内容 |
|------|------|
| サービス名 | リクルートX（RecruitX） |
| 一言説明 | `TBD` |
| ターゲット | `TBD` |
| 提供価値 | `TBD` |
| ドメイン | `recruitx.contentsx.jp` |

## 2. ページ構成

| ページ | ファイル | 主要JS | 状態 |
|--------|---------|--------|------|
| トップLP | `index.html` | `js/main.js`（ナビsticky/ハンバーガー/入場アニメ） | 制作中（ヒーローのみ） |
| お問い合わせ | `contact.html` | `js/main.js`（HubSpot Forms API v3 直送） | 実装済み・要テスト送信 |
| 導入事例 | `case.html` | `js/main.js` + `js/case.js`（業種フィルタ） | 実装済み |
| 料金プラン | `price.html` | `js/main.js`（入場アニメのみ） | 実装済み |

## 3. URL構造・ナビゲーション

- **共通ヘッダーナビ（全ページ統一・ブランド標準 `.rx-nav`）**: メニュー＝サービス / 事例 / 料金プラン / お役立ち情報 / コラム、CTA＝お問い合わせ。見た目は明朝ロゴ＋テキストリンク＋淡ピンクCTA（旧 `.rx-casepage` のモック準拠ナビは廃止）
  - 現在ページは `aria-current="page"` でピンク表示（事例ページの「事例」など）
  - 「料金プラン」は `price.html` へ遷移（2026-06-12 作成。全ページのヘッダー/フッターで `href` 有効化済み）。「お役立ち情報」「コラム」はページ未作成のため `href` 無し＋`aria-disabled="true"` のプレースホルダー（薄色・非遷移）。ページ作成時に `href` を付与
  - 「サービス」は index 内アンカー `#service`（他ページからは `index.html#service`）
- 現状のサイト内アンカー:
  - `#hero`（ヒーロー - sticky pin 350svh: 文字スワップ150svh → カーテンリビール100svh。背景固定のままAboutが下から覆う）
  - `#about`（**リクルートXとは** - 画像左/コピー右）
  - `#problem`（**こんなお悩みありませんか？** - 現場の声3連引用・明朝の大引用符）
  - `#service`（**3つのステップで採用を変える** - コンテンツ制作→SNS運用→Meta広告の縦フロー＋PLUSバンド「求人広告 運用代行」= Indeed等の運用代行・低コスト訴求）
  - `#company` / `#news`（旧ナビ項目の予約アンカー。共通ナビからは外したため当面は未使用。共通フッター（§5）も現状はこれらを使わずヘッダー同期のリンク構成）
- 導入事例は別ページ `case.html`（ナビ「事例」から遷移。`#case` アンカーは廃止）
  - デザインの出典: `採用/資料/デザイン候補/事例.png` のモックを再現し、青→ピンク `#ec2d87`／黒系 `#0a0a0c` に置換
  - 構成: ページヘッダー（右に写真・左へ白フェード）→ 業種フィルタ（すべて/飲食/清掃・家事代行）→ カード11枚（写真左38%・指標タイル3枚が写真に重なる）→ CTA（contact.html へ）
  - コンテナ幅（`.rx-chero__inner`/`.rx-clist__inner`）は `clamp(1200px, 92vw, 1760px)` で**広い画面ほど横を広く使う**（中央寄りすぎ解消）。ヘッダー写真は `min(56%, 1100px)`
  - 事例データは HTML 静的記述（出典: `採用/資料/事例/` の企画書PDF 3本）。数値は支援当時の実績
  - カード写真は `images/case/case-01〜11-*.jpg`（1448×1086 / sips jpeg 82%）。元PNGと未使用素材は `採用/資料/イメージ/` にアーカイブ
  - ヘッダーナビは全ページ共通（上記「共通ヘッダーナビ」を参照）。事例ページ固有のナビ上書きは廃止
  - 「詳しく見る」は詳細ページ未作成のためリンクではなく表示のみ（`p` 要素）
- お問い合わせは別ページ `contact.html` → ナビCTAから遷移
- 料金プランは別ページ `price.html`（ナビ「料金プラン」から遷移）
  - デザインの出典: ヒーロー＝`採用/資料/デザイン候補/料金ページ/１最新版.png`（2026-06-12 差し替え版）/ 以下＝同フォルダ `2〜5.png`
  - 構成: ①ヒーロー（**右半分に打ち合わせ写真をフルブリード背景で敷き、その上に浮遊カード2枚を重ねる**。左コピー「まずは30分、今の採用導線を一緒に整理しませんか？」＋CTA＋安心3ピル／写真上に「スタンダードプラン ¥98,000/月〜・人気No.1」カード＋「導入実績500社以上」カード。**PCは写真を絶対配置で右52vw(上限1500px)・ヘッダー(固定72px)の下から右下いっぱいに敷き、左下を大きな半径で丸くくり抜く（白オーバーレイは使わない）。コピーは中央寄せ枠をやめ画面左に詰め、幅・見出しフォント・本文を画面比率に応じて拡大（見出しはPCで最大68px=4.25rem、コピー幅最大880px、本文/ラベルも拡大。min-heightも46vwで広い画面ほど縦に大きく＝下の白を減らす）／SP・タブレット(≤960px)はコピー→フル幅写真バナーの縦積み。安心3ピルは≤560pxのみ2段折返し。スタンダードプランのカードは生成画像側（hero-meeting.webpに焼き込む想定）で対応し、プログラム側の浮遊カードは「導入実績500社以上」のみ（写真下部・顔に被らない位置）**）→ ②料金プラン4カード（A:フルスタート型 / B:広告強化型=人気No.1・ピンク枠 / C:コンテンツ単体型 / D:フリージョブ併用型）＋無料相談帯 → ③費用対効果（従来の求人広告 vs RecruitX 比較・20〜50%振り替え）→ ④導入5ステップ（薄グレー大パネル・点線コネクタ）→ ⑤最終CTA（白→ピンクグラデ背景・安心3ピル）
  - ⚠️ ヒーローのコピーは⑤最終CTAと同一（LPのCTAブックエンド型）。ヒーローの「¥98,000/月〜 スタンダードプラン」「導入実績500社以上」はモック由来の**新規の訴求文言**で、②のプランA〜D（40万/30万/25万…）や他資料に対応値が無い → 公開前に実数値の確認が必要
  - **旧ヒーローの3問プラン診断は廃止**（2026-06-12 最新版ヒーローへ差し替え時に削除。`js/price.js` も削除。復元はgit履歴 `b3d1675` 直後の版から）
  - ヒーロー写真は `images/price/hero-meeting.webp`（1672×941・16:9。**スタンダードプランのカードを右上に焼き込んだ完成画像**を使用。差し替え時は同比率で。PCは object-position 右寄せでカードを切らずに表示）
  - プランカードのアイコンは `images/price/plan-*.webp` 10枚（出典: `採用/資料/デザイン候補/料金ページ/素材/` のPNGを240px・WebP化）
  - 金額の出典: `採用/資料/SNSソーシャルリクルーティング_ご提案書パワポ.pdf`（プランA〜D。Cの漫画単価は 16,600円〜 に手動調整済み）
  - ページ専用スタイルは `css/price.css`（`--rxp-*` トークン。ヒーローのみ鮮やかピンク `--hp #f00858` をセクションスコープで使用。他セクションのピンクは `#eb2a63` 系）
- 新規LPは公開時に既存ページから内部リンク5〜10本 → [../content/SEO.md](../content/SEO.md)

## 4. URLパラメータ・特殊モード

- `TBD`（`?param=...` の仕様があれば記載）

## 5. 共通コンポーネント

- **共通フッター（全ページ複製・同一マークアップ `.rx-footer`）**: index / case / contact / price の4ページに**同一の`<footer>`ブロックを静的複製**（ヘッダー同様ビルドなし・`file://`で解決）。濃紺地（`--rx-text`）＋ブランドグラデの細い上線（`::before`）。
  - 構成: 上段＝ブランド（`Recruit X`明朝ロゴ＋タグライン）｜フッターナビ、下段＝会社情報＋著作権（罫線区切り）
  - フッターナビはヘッダーと同期: サービス（`index.html#service`）/ 事例（`case.html`）/ お問い合わせ（`contact.html`）/ 料金プラン（`price.html`）＋ お役立ち情報・コラムは `href`無し＋`aria-disabled="true"` の準備中プレースホルダー（薄表示・非遷移）
  - 会社情報: 「リクルートX（RecruitX）」＋「運営：コンテンツエックス株式会社 ／ Contents X Co., Ltd.」、著作権「© 2026 Contents X Co., Ltd.」
  - SNS・法的リンク（プライバシーポリシー/特商法）・コーポレートサイトリンクは**未確定のため当面は非掲載**。URL確定後に追加（追加時はこの節を更新）
  - SP（≤640px）は上段・下段とも縦積み
- **フローティングお問い合わせボタン（`.rx-fab`・全ページ共通／お問い合わせページを除く）**: 画面左下に固定し、200px以上スクロールしたら表示して以後ずっと追従（`contents-x.co.jp` の追従CTAに倣う）。
  - `js/main.js` が `<body>` 直下へ**DOM生成で挿入**（innerHTML不使用）。`#rx-contact-form` がある `contact.html` では挿入しない（＝自ページでは出さない）ため、ページ追加時も自動で付与・除外される
  - 見た目はブランドグラデのpill（メールアイコン＋「お問い合わせ」）。`href="contact.html"`。z-indexは `--z-fab`。`env(safe-area-inset-bottom)` 対応・`prefers-reduced-motion`/`print` 配慮
  - スタイルは `css/style.css` 末尾の `.rx-fab`（`.is-visible` で表示）
- ヘッダー / CTA / モーダル 等の仕様: `TBD`
- デザイン値は [DESIGN.md](DESIGN.md)、表示崩れ防止は [DEVICE-RULES.md](DEVICE-RULES.md) に従う

## 6. i18n（多言語対応）

- 対応有無: `TBD`
- 実装する場合の既存パターン（Contents X 系共通）:
  - 2層構造: JSON辞書（テキスト走査で日→英置換）+ `data-ja`/`data-en`（辞書より優先）
  - スクリプト読込順序は **`i18n → nav` を厳守**（崩すと言語切替が壊れる）
  - `translateAll()` は言語判定してから呼ぶ（無条件呼び出し禁止）
  - localStorageキーは `rx-lang`（サービス専用）
  - EN は日本語より約30%長い前提でナビ/ボタン幅を設計

## 7. 外部サービス連携

- 設定値の正本は [../operations/INFRA.md](../operations/INFRA.md)。ここでは「どの連携を使うか」のみ管理。
- 使用予定: GA4（新規）/ Clarity（新規）/ HubSpot フォーム（`TBD`）

## 8. デプロイ

- GitHub Pages / CNAME `recruitx.contentsx.jp` / DNS お名前.com
